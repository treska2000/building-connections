# Turso (required for Vercel serverless API)
# Create a database at https://turso.tech and run: turso db show <db-name> --url
# Create a token: turso db tokens create <db-name>
TURSO_DATABASE_URL=https://your-db-your-org.region.turso.io
TURSO_AUTH_TOKEN=your-auth-token
